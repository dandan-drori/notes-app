import { MultilineNoteTextPipe } from './multiline-note-text.pipe';

describe('MultilineNoteTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MultilineNoteTextPipe();
    expect(pipe).toBeTruthy();
  });
});
